<header class="navbar navbar-toggleable-md navbar-light bg-faded">
   <nav class="container">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle admin navigation">
         <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
         <div class="navbar-nav">
            <a class="nav-item nav-link {{ myheader.class == 'adminReferencesManagement' ? 'active'}}"
               href="{{path_for("adminReferencesManagement")}}">
               <i class='fa fa-cog'></i>
               References</a>
            <a class="nav-item nav-link {{ myheader.class == 'adminUsersManagement' ? 'active'}}"
                href="{{path_for('adminUsersManagement')}}">
               <i class='fa fa-cog'></i>
               Users</a>
         </div>
      </div>
   </nav>
</header>
